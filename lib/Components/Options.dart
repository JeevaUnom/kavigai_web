// options.dart
import 'package:flutter/material.dart';
// ignore: unused_import
import '../Pages/Home.dart';

class Options extends StatelessWidget {
  const Options({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        // Your Options widget UI here
        );
  }
}
